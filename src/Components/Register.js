import React from 'react';
import ReactBootstrapForm from './ReactBootstrapForm';

const Register = () => {
    return (
        <div>
            <ReactBootstrapForm></ReactBootstrapForm>
        </div>
    );
};

export default Register;